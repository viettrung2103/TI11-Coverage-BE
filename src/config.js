const DB_HOST = "localhost"; // the db host name
const DB_USERNAME = "root"; // the database username
const DB_PASSWORD = ""; // the database password
const DB_NAME = "tidb"; // the database name
const DB_PORT = "3300"; // the database port

const HOST = "localhost"; // the host name for the http server
const PORT = 8080; // the port of the http server

module.exports = {
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_USERNAME,
  DB_PORT,
  HOST,
  PORT,
};
